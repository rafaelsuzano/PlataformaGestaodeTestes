import { Box, Typography } from '@mui/material';

export default function Corrigir() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Correção de Falhas</Typography>
      <Typography variant="body1">
        Funcionalidade de correção (orrigier) em desenvolvimento.
      </Typography>
    </Box>
  );
}
